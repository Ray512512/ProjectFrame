package com.ray.library.utils;


/**
 * Created by Ray on 2017/12/20.
 */

public class LocationUtil {

/*
    public static String getDistance(LatLng lng){
        String distance="";
        if(MiPaiLifelication.getLocation()==null)return distance;
        LatLng myL=new LatLng(MiPaiLifelication.getLocation().getLatitude(),MiPaiLifelication.getLocation().getLongitude());
        float location = AMapUtils.calculateLineDistance(myL,lng);
        if(location==-1D){
            return distance;
        }
        if(location>1000){
            String a=new DecimalFormat("##0.0").format((location)/1000);
            return a+"km";
        }else {
            return new DecimalFormat("##0.0").format((location))+"m";
        }
    }

    public static float getDistanceR(LatLng lng){
        float distance=-1;
        if(MiPaiLifelication.getLocation()==null)return distance;
        LatLng myL=new LatLng(MiPaiLifelication.getLocation().getLatitude(),MiPaiLifelication.getLocation().getLongitude());
        float location = AMapUtils.calculateLineDistance(myL,lng);
        return location;
    }*/

}
